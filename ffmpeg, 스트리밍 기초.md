---
title: ffmpeg, 스트리밍 기초
aliases:
  - FFmpeg 기초
  - HLS 기초
  - ABR 스트리밍 기초
tags:
  - video-streaming
  - ffmpeg
  - hls
  - codec
  - abr
created: 2026-09-02
up: "[[README]]"
---

# ffmpeg, 스트리밍 기초

> [!abstract] 요약
> 코덱과 컨테이너의 차이, GOP/키프레임 구조, 비트레이트 제어(CBR·VBR·CRF·VBV), HLS/ABR 패키징, 그리고 브라우저 재생 제약까지 스트리밍 파이프라인의 기초를 정리한 노트.

> [!tldr] 한 줄로 연결하면
> **코덱**이 비트를 줄이고 → **GOP**가 "어디서 끊고 다시 시작할지"를 정하고 → **비트레이트 제어**가 그 비트를 시간에 따라 어떻게 뿌릴지 정하고 → **HLS**가 그걸 짧은 파일과 목록으로 나눠 나르고 → **브라우저·`hls.js`** 가 그 조각을 다시 연속 재생으로 붙인다.

---

## 1. 코덱 & 컨테이너 (Codec / Container)

### 1) 층이 다른 두 개념

- **Codec (Coder-Decoder)**: 픽셀·샘플을 **비트스트림**으로 줄이는 압축 규칙. 인코더가 만들어 내는 결과물이 바로 이 압축된 비트스트림이다.
- **Container Format**: 그 비트스트림과 오디오, 자막, 챕터, 타임스탬프를 **하나의 파일 규격으로 묶는 상자**. "스트림을 어디에 어떻게 담을지"의 규칙.

> [!warning] "코덱이 뭐예요?"와 "확장자가 뭐예요?"는 다른 질문
> 재생 불가 이슈는 대부분 **코덱·프로파일·레벨 미지원**이지 파일 이름 끝이 아니다. 확장자만 바꿔서는 해결되지 않는 게 정상에 가깝다. 플레이어가 못 여는 원인을 컨테이너에서 찾기 전에 코덱부터 확인할 것.

### 2) 비디오 코덱 비교

| 코덱 | 압축 효율 | 호환성 | 트레이드오프 |
| --- | --- | --- | --- |
| **H.264 (AVC)** | 기준점 | 거의 모든 기기에서 재생 | 호환성 우선일 때 기본 후보 |
| **H.265 (HEVC)** | 같은 화질에 비트레이트 절감 | 중간 (기기·브라우저 편차) | 인코딩 비용 ↑, 라이선스 이슈 |
| **AV1** | 가장 높은 편 | 낮음 (신형 기기 위주) | 인코딩 시간이 크게 늘어남 |

- 같은 장면이라도 코덱마다 **압축 효율 · 디코더 요구사항 · 라이선스**가 다르다.
- H.265·AV1은 같은 체감 화질에 비트레이트를 더 줄일 여지가 있지만, **인코딩 시간 · 하드웨어 지원 · 라이선스 · 구형 단말 재생 여부** 가 함께 따라온다. "효율이 좋으니 무조건"이 성립하지 않는 지점.

### 3) 오디오 코덱

- **AAC**: `.mp4` · HLS 조합에서 무난한 기본값.
- **Opus**: 낮은 비트레이트에서도 말·음악 모두 선전하는 편. 다만 컨테이너·플레이어 조합을 한 번 더 확인해야 하는 경우가 있다.

### 4) 컨테이너

| 컨테이너 | 성격 | 주 사용처 |
| --- | --- | --- |
| `.mp4` | 범용 | 웹·모바일 전달 |
| `.mkv` | 트랙 구성이 자유로움 | 여러 오디오·자막 트랙을 느슨하게 담을 때 |
| `.ts` (MPEG-TS) | "끊어서 흘리기"에 맞춤 | 방송 계통, HLS 세그먼트 |

---

## 2. 프레임 구조와 GOP (Group of Pictures)

### 1) 왜 프레임을 I/P/B로 나누는가

동영상 압축의 핵심은 **"매 프레임을 통째로 저장하지 않는다"** 는 점이다. 매 프레임을 독립 이미지로 저장하면 용량이 바로 폭발하는데, 대부분의 장면은 배경이 같고 전경만 조금 움직인다. 변화가 작은 구간은 **차이만** 남긴다.

- **I-Frame (Intra-coded / Keyframe)**: 다른 프레임을 참조하지 않는 **완전한 한 장의 정지 이미지** (JPEG에 가까움)
	- 그 순간을 통째로 다시 그리므로, 에러가 쌓여도 여기서 **리셋**된다.
	- 타임라인 아무 데나 점프할 때의 **디코딩 시작점** → Seek, 끊김 복구의 기준.
	- HLS 스트리밍 시 영상 조각(`.ts`)을 나누는 **절대적인 분할 기준점**.
- **P-Frame (Predicted Frame)**: 이전 I 또는 P 프레임 대비 "뭐가 움직였는지"만 기록
	- 참조가 잘못되면 이후 P/B도 같이 깨진다 → **화면이 한동안 깨지다가 다음 I에서 회복**되는 현상이 이것.
- **B-Frame (Bi-directional Predicted Frame)**: 앞·뒤 프레임을 모두 참조해 예측, 보통 더 작게 떨어짐
	- 대신 **디코딩 순서와 표시 순서가 어긋나** 인코더·디코더가 프레임을 재정렬해야 한다.
	- 압축 효율은 좋아도 **지연(latency)과 구현 복잡도가 올라가서**, 라이브·초저지연 파이프라인에서는 B를 줄이거나 끄는 선택이 나온다.

### 2) GOP 길이의 트레이드오프

**GOP**는 I-Frame으로 시작해 다음 I-Frame 직전까지의 프레임 그룹.

| GOP | 장점 | 대가 |
| --- | --- | --- |
| **짧게** | 언제든 근처 I로 점프 → Seek·편집·구간 반복·적응 스트리밍에 유리 | 용량 증가 (I가 자주 들어감) |
| **길게** | 평균 압축 효율 향상 | 키프레임까지 되감기가 느려지고, 손상 시 복구도 늦고, 세그먼트 독립성이 깨지기 쉬움 |

> [!example] GOP 길이 계산과 세그먼트 정렬
> 30fps 영상에서 `-g 60` → GOP 길이 60프레임(= **2초**), 정확히 2초 간격으로 Keyframe 배치.
> HLS `-hls_time 2`와 맞추는 이유가 여기 있다.

> [!warning] 조각 첫 프레임이 I가 아니면
> 세그먼트 중간에만 I가 있거나 조각이 이전 조각을 참조해야 열리면, **그 조각만 받아서는 화면을 못 연다.** 이전 조각을 빌려야 하는 의존이 생기고, 중간부터 재생·화질 전환이 불안정해진다.

---

## 3. 비트레이트 제어 — '얼마나'가 아니라 '어떻게' 쓸지

### 1) 세 가지 방식

| 방식 | 동작 | 잘 맞는 곳 | 실패 모드 |
| --- | --- | --- | --- |
| **CBR** (Constant Bitrate) | 초당 비트량을 거의 고정 (파이프 굵기 고정) | 라이브·중계·방송 장비처럼 대역폭 예산을 미리 잡아야 할 때 | 잔잔한 장면에 비트가 남고, 폭파·스포츠 구간에선 모자라 깨짐 |
| **VBR** (Variable Bitrate) | 어려운 구간에 비트를 몰아줌 | VOD·다운로드 배포. 전체 용량 대비 체감 화질이 좋은 편 | 순간 피크가 제한 없이 솟으면 버퍼·네트워크가 먼저 한계에 닿음 |
| **CRF** (Constant Rate Factor) | "이 정도 화질 유지"를 목표로, 비트레이트는 **결과로** 받음 | 화질 우선 인코딩 | "항상 N Mbps"가 필요한 송출에는 단독으로 부족 |

### 2) CRF 값 감각

- H.264 기준 범위 `0 ~ 51`. 0에 가까울수록 거의 무손실, **기본값 `23`** 전후, **실무 구간 `18 ~ 28`**.
- CRF 값이 낮을수록 화질 향상/용량 증가, 높을수록 화질 저하/용량 감소.
- **같은 CRF라도 파일 크기가 크게 달라진다.** 애니메이션처럼 평탄한 화면은 작고, 노이즈·디테일 많은 실사는 커진다. 소스·해상도·장르에 따라 결과가 갈림.

### 3) 평균이 아니라 피크 — VBV 상한

스트리밍용으로 넘길 때는 CRF만으로 끝내지 않고 `-maxrate` · `-bufsize` 같은 **VBV(Video Buffer Verifier) 상한**을 걸어 "순간적으로 얼마 이상 나가지 못하게" 막는 경우가 많다. 버퍼 언더런·재버퍼링을 줄이는 쪽.

> [!important] 스트리밍에서 중요한 건 평균만이 아니다
> 평균 3Mbps여도 **0.5초만 8Mbps로 튀면** 모바일망·약한 Wi-Fi에서 버퍼가 출렁인다. 짧은 구간의 피크를 잡는 게 VBV의 역할.

### 4) ABR 래더(ladder) 간격 설계

멀티 화질 패키징에서는 각 트랙이 **"이 대역폭 구간의 대표"** 가 되도록 간격을 벌린다.

- 1080p와 720p 비트레이트가 **너무 붙어 있으면** → ABR이 바꿔 봐야 이득이 작다.
- **너무 떨어져 있으면** → 한 단계만 내려가도 화질 낭떠러지가 크다.

---

## 4. HLS 및 가변 비트레이트 (ABR: Adaptive Bitrate Streaming)

### 1) 파일을 잘라 파는 방식

Apple이 제안한 표준 프로토콜. "한 방짜리 mp4" 대신 수 초 단위의 짧은 미디어 조각(`.ts` 또는 `.m4s`)과 이를 가리키는 텍스트 인덱스(`.m3u8`)를 **HTTP 기반**으로 전송한다. 덕분에 CDN·캐시·기존 웹 인프라를 그대로 쓰기 쉽다.

- 시청자는 전체 영상이 아니라 **"지금 필요한 몇 초치 조각"** 만 받는다.
- 중간에 들어와도 처음부터 받을 필요가 없고, CDN 캐시도 **조각 단위로 히트**시키기 쉽다.

```text
                  ┌─── v0/prog_index.m3u8 (1080p) ─── segment_000.ts, ...
                  │
[master.m3u8] ────┼─── v1/prog_index.m3u8 (720p)  ─── segment_000.ts, ...
 (Master Index)   │
                  └─── v2/prog_index.m3u8 (480p)  ─── segment_000.ts, ...
```

### 2) 두 종류의 플레이리스트

- **`master.m3u8`** — 메뉴판 / 후보 목록
	- 각 variant에 `BANDWIDTH`(대역폭), `RESOLUTION`(해상도) 힌트를 붙여 두어 플레이어가 **초기 선택**을 하게 한다.
	- 너무 낙관적인 `BANDWIDTH`를 적어 두면 시작부터 버거운 트랙을 고를 수 있다.
- **`prog_index.m3u8`** (media playlist) — 한 화질 안의 시간 순 조각 목록
	- `#EXTINF:2.000000`으로 조각 길이, 그 아래 줄에 `segment_000.ts` 같은 실제 파일 경로.
	- **VOD**는 끝(`#EXT-X-ENDLIST`)이 있고, **라이브**는 창이 밀리듯(sliding window) 갱신된다.

### 3) ABR이 실제로 하는 동작

클라이언트 플레이어가 **다운로드 속도·버퍼 상태**를 보다가 더 높은 트랙으로 올리거나, 버티기 힘들면 낮춘다. 네트워크가 흔들려도 **끊김 대신 화질이 먼저 움직인다.**

> [!note] 세그먼트 길이의 타협점
> ABR 전환은 보통 **"다음 조각부터"** 일어난다.
> - 조각이 **길면** → 화질 전환 반응이 둔하다.
> - 너무 **짧으면** → 요청 횟수·오버헤드가 늘어난다.
>
> **2초 전후**가 실무에서 자주 보이는 타협점.

### 4) 트랙 전환이 매끄러우려면

- 해상도만 다르고 **타임라인·키프레임 주기가 잘 맞춰져** 있어야 유리 → 멀티 비트레이트 패키징 때 GOP·세그먼트 길이를 통일하는 이유.
- 전환이 눈에 띄게 깜빡인다면 **키프레임 정렬 · 타임스탬프 · 오디오 갭** 문제일 때가 많다. 영상만 여러 화질로 만들고 오디오 타임라인이 어긋나도 전환 순간 이질감이 생긴다.
- **independent segment**가 중요한 이유: 각 조각이 혼자 디코딩 가능해야 화질을 바꿔 받거나, Seek 후 첫 조각만으로 화면을 바로 열 수 있다.

---

## 5. FFmpeg 파이프라인에서 실제로 하는 일

### 1) 영상 메타데이터 검증 (`ffprobe`)

```bash
# 영상 비디오/오디오 코덱 정보 확인
ffprobe -hide_banner sample.mp4
```

```powershell
# Keyframe 간격 및 타임스탬프 검증 (PowerShell)
ffprobe -v error -select_streams v:0 -show_entries frame=key_frame,best_effort_timestamp_time -of compact output_gop60.mp4 | Select-String "key_frame=1"
```

코덱·해상도뿐 아니라 **`key_frame=1`인 프레임의 시각**을 찍어 보면, 의도한 GOP 간격으로 키가 박혔는지 검증할 수 있다.

### 2) ABR HLS 패키징 파이프라인 (`ffmpeg`)

단일 원본 영상(`sample.mp4`)을 입력받아 1080p, 720p, 480p 3가지 화질 트랙과 `master.m3u8`로 일괄 패키징한다.

```bash
ffmpeg -i sample.mp4 \
  -filter_complex "[0:v]split=3[v1][v2][v3]; [v1]scale=w=1920:h=1080[v1out]; [v2]scale=w=1280:h=720[v2out]; [v3]scale=w=854:h=480[v3out]" \
  -map "[v1out]" -c:v:0 libx264 -b:v:0 5000k -maxrate:v:0 5350k -bufsize:v:0 7500k \
  -map "[v2out]" -c:v:1 libx264 -b:v:1 2800k -maxrate:v:1 2996k -bufsize:v:1 4200k \
  -map "[v3out]" -c:v:2 libx264 -b:v:2 1400k -maxrate:v:2 1498k -bufsize:v:2 2100k \
  -map a:0 -c:a:0 aac -b:a:0 192k -map a:0 -c:a:1 aac -b:a:1 128k -map a:0 -c:a:2 aac -b:a:2 96k \
  -var_stream_map "v:0,a:0 v:1,a:1 v:2,a:2" \
  -f hls -hls_time 2 -hls_playlist_type vod \
  -hls_flags independent_segments -hls_segment_type mpegts \
  -hls_segment_filename "v%v/segment_%03d.ts" \
  -master_pl_name master.m3u8 "v%v/prog_index.m3u8"
```

> [!tip] 옵션이 하는 일
> - **`-filter_complex`**: 입력(`[0:v]`)을 `split=3`으로 복제한 뒤 각각 1080p/720p/480p로 `scale`. **한 번 디코딩한 소스를 여러 화질로 빼는** 전형적인 ABR 전처리.
> - **`-b:v` / `-maxrate:v` / `-bufsize:v`**: 화질마다 VBV 제어를 적용. 버퍼 크기(`bufsize`)를 제어해 대역폭 변동 시 비트레이트가 순간적으로 튀는 문제(Spike)를 방지 → 스트리밍 안정성 확보.
> - **오디오 192k / 128k / 96k**: 트랙에 맞게 계단식으로 나눈다. **저화질 트랙에 고비트 오디오를 붙일 필요는 보통 없다.**
> - **`-var_stream_map`**: "이 비디오 + 이 오디오"를 한 세트(variant)로 묶어 멀티 트랙 구성.
> - **`-f hls -hls_time 2 -hls_playlist_type vod -hls_flags independent_segments -hls_segment_type mpegts`**: "**2초 단위 TS 조각 + VOD용 재생목록 + 조각 독립 디코딩**"을 목표로 한 조합.

---

## 6. 웹 클라이언트 재생 — 서버 인코딩과는 다른 층

### 1) `file://` 와 CORS

플레이리스트와 조각을 브라우저가 `fetch`로 가져가기 때문에, 로컬 파일 경로로 HTML을 열면 페이지 origin이 `file://`이 되어 **같은 폴더의 `.m3u8` · `.ts`라도 보안 정책에 걸린다.** "파일이 있는데 재생이 안 됨"의 상당수가 이 층.

- 해결: Python 내장 서버(`python -m http.server 8000`) 등으로 **HTTP origin** 구축.
- HTTP로 올려도 **CORS 헤더가 없으면** 다른 도메인 플레이어·개발 서버 조합에서 막힌다.

### 2) MSE (Media Source Extensions) & `hls.js`

- **Safari 계열**은 HLS를 자체적으로 비교적 잘 처리하는 편.
- **Chromium · Firefox 계열 데스크톱**은 `<video src="*.m3u8">`만으로 부족한 경우가 많고, **MSE**(미디어를 JS가 조각 단위로 밀어 넣는 API)에 의존한다.
- **`hls.js`**: 플레이리스트를 파싱하고 → 조각을 받고 → TS 같은 형태를 브라우저가 소화하기 쉬운 컨테이너(흔히 `fMP4`)로 바꾼 뒤 → `SourceBuffer`에 append. 이 중간 변환이 **transmux**이며, 여기서 실패하면 **코덱이 맞아도 재생이 안 된다.**

> [!bug] 디버깅은 층을 나눠서
> 인코딩 재작업 전에 네트워크 탭에서 playlist·segment의 상태 코드와 헤더부터 볼 것.
> 1. 조각 URL이 **200**으로 내려오는가
> 2. **playlist 문법**이 맞는가
> 3. **코덱·해상도**가 브라우저 지원 범위인가
> 4. **transmux / MSE** 단계에서 에러가 나는가
>
> 미디어 문제인지 웹 문제인지 나눠 보는 게 핵심.

---

## 7. Next Step Topics (운영 단계로 갈수록 커지는 주제)

> [!todo] 파고들 심화 파트
> 1. **fMP4 / CMAF 세그먼팅**
> 	- "HLS용 TS"와 "DASH용 조각"을 이중으로 쌓지 않고 **공통 조각**에 가깝게 가져가려는 흐름.
> 	- 헤더 효율 · 프리로드 · 멀티 코덱 대응에서 이득을 보는 경우가 있음.
> 2. **트랜스코딩 파이프라인 가속 (GPU 인코딩)**
> 	- NVIDIA `NVENC` / Intel `QuickSync`는 **동시 트랜스코딩 수(처리량)** 를 늘릴 때 가치가 크다.
> 	- 다만 같은 비트레이트에서 `x264` 대비 **화질 곡선이 다르고**, 구형 GPU 동작도 달라 "빠르니 무조건"이 아니다. 목표 화질·동시 인코딩 수에 맞춰 고르고, **배포용 마스터**와 **미리보기용 빠른 인코드**를 나누기도 함.
> 3. **Signed URL / 토큰 기반 콘텐츠 보안**
> 	- 조각 URL이 고정이면 **공유만으로 원본에 가까운 다운로드**가 가능.
> 	- CDN(CloudFront 등) 레벨에서 만료 시간이 있는 URL·쿠키 토큰으로 우회를 어렵게 만든다.
> 	- 재생 플레이어는 만료 전에 조각을 받고, 새로고침·공유 링크는 막히는 식의 **UX 설계가 같이 간다.**

---

## 관련 노트

- [[README]] — 전체 학습 로드맵
