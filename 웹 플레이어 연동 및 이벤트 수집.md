---
title: 웹 플레이어 연동 및 이벤트 수집
aliases:
-  hls.js 플레이어
- 시청 로그 수집
- ABR 수동 제어
tags:
-  video-streaming
-  hls-js
-  javascript
-  recsys-log
created: 2026-09-03
up: "[[README]]"
---

# 웹 플레이어 연동 및 이벤트 수집

> [!abstract] 요약
> `hls.js` 라이브러리를 활용해 브라우저에 HLS 플레이어를 탑재하고, 유저의 수동 화질 제어 기능 및 추천 시스템 학습용 시청 행동 데이터(Implicit Feedback) 수집 이벤트를 구축한 기록.

> [!tldr] 한 줄로 연결하면
> 브라우저가 **`hls.js`** 로 마스터 플레이리스트를 파싱하면 → 유저가 **화질 드롭다운**으로 특정 트랙을 고르거나 Auto ABR을 동작시키고 → **Heartbeat·Ended 이벤트**가 유저의 시청 구간과 완독 여부를 정밀하게 수집해 추천 모델의 학습 Signal로 연결된다.
> 
> 

---

## 1. HLS 웹 플레이어 Architecture

### 1) 브라우저 호환성과 MSE (Media Source Extensions)

* 데스크톱 Chrome/Firefox 등 대부분의 환경에서는 `<video src="*.m3u8">`을 직접 실행하지 못함.


* **`hls.js`**: `master.m3u8`을 JavaScript 레벨에서 파싱한 후, 미디어 조각(`.ts`)을 받아 브라우저가 읽을 수 있는 형태(`fMP4`)로 트랜스머징(transmux)하여 `MediaSource`에 주입함.



```text
[master.m3u8] ──> hls.js 파싱 ──> [.ts Segment 다운로드] ──> Transmux (JS) ──> SourceBuffer ──> <video> 재생

```

---

## 2. ABR 수동 화질 선택 (Quality Selector)

### 1) 동적 화질 목록 구성

`hls.js`가 Manifest 파일 파싱을 마치면 `Hls.Events.MANIFEST_PARSED` 이벤트가 발생하며, `hls.levels` 배열에 1080p, 720p, 480p 등 이용 가능한 트랙 정보가 매핑됨.

```javascript
hls.on(Hls.Events.MANIFEST_PARSED, function () {
  hls.levels.forEach((level, index) => {
    const option = document.createElement('option');
    option.value = index; // 트랙 인덱스 (0, 1, 2)
    option.text = `${level.height}p (${Math.round(level.bitrate / 1000)}kbps)`;
    qualitySelect.appendChild(option);
  });
});

```

### 2) Level Switching 제어

* **`hls.currentLevel = -1`**: 자동 변환 (Auto ABR) 모드.


* **`hls.currentLevel = N`**: 유저가 지정한 특정 레벨로 화질 고정.



---

## 3. 추천 시스템용 시청 Signal 수집 (Implicit Feedback)

추천 알고리즘(Part 2)의 타깃 라벨과 피처 데이터 생성을 위해 클라이언트 단에서 수집하는 3가지 핵심 이벤트.

| 이벤트 종류 | 트리거 조건 | 수집 목적 및 데이터 |
| --- | --- | --- |
| **`LEVEL_SWITCHED`** | ABR 자동 변경 또는 유저 수동 변경 시 | 네트워크 환경 및 유저의 화질 선호도 수집 |
| **`Heartbeat` (`timeupdate`)** | 재생 중 5초 간격으로 주기적 발생 | 실제 시청 구간, 중간 이탈 지점(`lastPosition`) 추적

 |
| **`Ended` (`ended`)** | 영상 재생이 완전히 종료되었을 때 | 추천 모델의 최우선 Positive Target인 **완독 여부(`isCompleted = true`)**<br> |

### 1) Heartbeat 수집 로직 (중복 방지)

`timeupdate` 이벤트는 초당 여러 번 과도하게 발생하므로 정수 단위 5초 간격으로 필터링 처리함.

```javascript
video.addEventListener('timeupdate', () => {
  const currentTime = video.currentTime;
  
  if (Math.floor(currentTime) % 5 === 0 && Math.floor(currentTime) !== lastCheckTime) {
    lastCheckTime = Math.floor(currentTime);
    watchLog.lastPosition = Number(currentTime.toFixed(2));
    console.log(`[Event: Heartbeat] 재생 위치: ${watchLog.lastPosition}초 | 화질: ${watchLog.currentQuality}`);
  }
});

```

---

## 4. 실습 코드 (`index.html`)

```html
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>HLS Video Player with Quality Selector</title>
  <script src="https://cdn.jsdelivr.net/npm/hls.js@latest"></script>
  <style>
    body { background-color: #121212; color: #fff; font-family: sans-serif; display: flex; flex-direction: column; align-items: center; padding-top: 40px; }
    video { width: 800px; max-width: 90%; border-radius: 8px; box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5); }
    .controls { margin-top: 15px; display: flex; align-items: center; gap: 10px; }
    select { background-color: #1e1e1e; color: #fff; border: 1px solid #444; padding: 8px 12px; border-radius: 4px; font-size: 14px; cursor: pointer; }
  </style>
</head>
<body>

  <h1>🎬 HLS Player with Quality Selector</h1>
  
  <video id="video" controls></video>
  
  <div class="controls">
    <label for="qualitySelect">⚙️ 화질 설정:</label>
    <select id="qualitySelect">
      <option value="-1">자동 (Auto ABR)</option>
    </select>
  </div>

  <script>
    const video = document.getElementById('video');
    const qualitySelect = document.getElementById('qualitySelect');
    const videoSrc = 'master.m3u8';

    let watchLog = {
      videoId: 'sample_video_01',
      lastPosition: 0,
      isCompleted: false,
      currentQuality: 'Auto'
    };

    let lastCheckTime = 0;

    if (Hls.isSupported()) {
      const hls = new Hls();
      hls.loadSource(videoSrc);
      hls.attachMedia(video);

      hls.on(Hls.Events.MANIFEST_PARSED, function () {
        console.log("[System] HLS Manifest 로드 완료");
        hls.levels.forEach((level, index) => {
          const option = document.createElement('option');
          option.value = index;
          option.text = `${level.height}p (${Math.round(level.bitrate / 1000)} kbps)`;
          qualitySelect.appendChild(option);
        });
      });

      qualitySelect.addEventListener('change', (event) => {
        const levelIndex = parseInt(event.target.value);
        hls.currentLevel = levelIndex;
        console.log(`[User Control] 화질 설정 변경 -> ${levelIndex === -1 ? 'Auto' : hls.levels[levelIndex].height + 'p'}`);
      });

      hls.on(Hls.Events.LEVEL_SWITCHED, function (event, data) {
        const levelInfo = hls.levels[data.level];
        watchLog.currentQuality = `${levelInfo.height}p`;
        console.log(`[Event: Quality Switch] 현재 재생 화질 -> ${watchLog.currentQuality}`);
      });

    } else if (video.canPlayType('application/vnd.apple.mpegurl')) {
      video.src = videoSrc;
    }

    video.addEventListener('timeupdate', () => {
      const currentTime = video.currentTime;
      if (Math.floor(currentTime) % 5 === 0 && Math.floor(currentTime) !== lastCheckTime) {
        lastCheckTime = Math.floor(currentTime);
        watchLog.lastPosition = Number(currentTime.toFixed(2));
        console.log(`[Event: Heartbeat] 위치: ${watchLog.lastPosition}초 | 화질: ${watchLog.currentQuality}`);
      }
    });

    video.addEventListener('ended', () => {
      watchLog.isCompleted = true;
      console.log("%c[Event: Completed] 완독 달성!", "color: #00ff00; font-weight: bold;");
      console.log("[Final Watch Log]", watchLog);
    });
  </script>

</body>
</html>

```

---

## 관련 노트

* [[ffmpeg, 스트리밍 기초]] — ABR 패키징 및 HLS 기본 원리
* [[README]] — 전체 학습 로드맵